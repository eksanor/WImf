allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}

// buildscript {
//     repositories {
//         google()  // Ensure Google repository is included
//         mavenCentral()  // Optional, already defined in allprojects block
//     }

//     dependencies {
//         classpath 'com.android.tools.build:gradle:8.2.1'  // or your current Android plugin version
//         classpath 'com.google.gms:google-services:4.3.15'  // Add this line for Firebase
//     }
// }